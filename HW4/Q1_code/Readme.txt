========================================
Assuming the lc1 and lc2 
sc1 and sc2 and the connectivity between the lc1 and lc2 exist via sc1 & sc2 
Using the configurations made in the part1 and all the routes and ips of lc1 &lc2 || sc1 & sc2

Attaching the topology of lc1 lc2 & sc1 sc2 in the document.

Attaching the "Dockerfile" where all the packages are installed 
In the script containers are created using the  "iserver" image which is built on top of ubuntu using the dockerfile(packages are installed from docker file)

running the script:

python "q1_part2.py"
it expects a Input which should be in the  form of "data.csv" -------------- "<file_name>"

